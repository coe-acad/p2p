# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
node_modules

# env — never commit env files; real values live in Vercel project settings
.env
.env.*
!.env.example

# Capacitor — generated native projects (re-created by `npx cap add android` / sync)
android
ios

# Signing secrets — never commit, even if the android/ ignore is later relaxed.
# Losing these means losing the ability to publish updates to the same Play
# Store listing, so back them up outside the repo (password manager + offline).
*.jks
*.keystore
keystore.properties
